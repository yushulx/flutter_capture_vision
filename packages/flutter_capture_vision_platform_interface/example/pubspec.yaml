name: flutter_capture_vision_platform_interface_example
description: Exercises the shared value types and contracts of flutter_capture_vision_platform_interface.
publish_to: 'none'
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.10.0
  flutter: '>=3.38.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_capture_vision_platform_interface: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
